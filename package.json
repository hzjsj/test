{
  "name": "api-inspector",
  "version": "1.0.0",
  "description": "捕获和筛选网络 API 请求的 Chrome 扩展",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "tsc && rm -rf dist && vite build && BUILD_TARGET=service-worker vite build && BUILD_TARGET=devtools vite build && BUILD_TARGET=panel vite build && BUILD_TARGET=popup vite build && BUILD_TARGET=content-script vite build && node scripts/postbuild.js",
    "preview": "vite preview"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.287",
    "@types/react": "^18.3.12",
    "@types/react-dom": "^18.3.1",
    "@types/turndown": "^5.0.6",
    "@vitejs/plugin-react": "^4.3.4",
    "typescript": "^5.6.0",
    "vite": "^6.0.0"
  },
  "dependencies": {
    "@ant-design/icons": "^5.5.1",
    "antd": "^5.22.0",
    "katex": "^0.16.46",
    "mammoth": "^1.12.0",
    "react": "^18.3.1",
    "react-dom": "^18.3.1",
    "turndown": "^7.2.4"
  }
}
